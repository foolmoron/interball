using UnityEngine;
using System.Collections;

public class StaticColor : MonoBehaviour {
    // Add this to a SpriteRenderer object parented under a Board in order to ignore the global color changing effect
}